{
  "name": "pensieve-tools",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "publish": "npm run build && node dist/publish.js"
  },
  "dependencies": {
    "basic-ftp": "^5.0.0",
    "dotenv": "^16.0.0",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "typescript": "^5.2.0",
    "ts-node-dev": "^2.0.0",
    "@types/js-yaml": "^4.0.5"
  }
}
